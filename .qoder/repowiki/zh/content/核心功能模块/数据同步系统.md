# 数据同步系统

<cite>
**本文档引用的文件**
- [app.js](file://app.js)
- [server.js](file://server.js)
- [lib/db.js](file://lib/db.js)
- [lib/auth.js](file://lib/auth.js)
- [lib/aiComment.js](file://lib/aiComment.js)
- [netlify/functions/comment.js](file://netlify/functions/comment.js)
- [package.json](file://package.json)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)
10. [附录](#附录)

## 简介

MyScore 是一个基于 Web 的成绩记录与管理工具，具备云端数据同步功能。本文档深入分析其数据同步系统的完整实现，包括云端同步机制、本地存储策略、数据合并算法以及相关的错误处理和网络异常恢复机制。

系统采用客户端-服务器架构，客户端负责本地数据存储和用户界面交互，服务器端负责用户认证、数据存储和同步服务。数据同步采用双向合并策略，确保本地和云端数据的一致性。

## 项目结构

MyScore 项目采用模块化设计，主要包含以下核心模块：

```mermaid
graph TB
subgraph "客户端层"
A[app.js - 主应用逻辑]
B[index.html - 用户界面]
C[style.css - 样式表]
end
subgraph "服务器层"
D[server.js - 主服务器]
E[lib/db.js - 数据库操作]
F[lib/auth.js - 认证服务]
G[lib/aiComment.js - AI评论服务]
end
subgraph "函数服务"
H[netlify/functions/comment.js - Netlify函数]
end
subgraph "配置文件"
I[package.json - 项目配置]
J[netlify.toml - 部署配置]
end
A --> D
B --> A
C --> A
D --> E
D --> F
D --> G
H --> G
```

**图表来源**
- [app.js:1-50](file://app.js#L1-L50)
- [server.js:1-50](file://server.js#L1-L50)
- [lib/db.js:1-30](file://lib/db.js#L1-L30)
- [lib/auth.js:1-30](file://lib/auth.js#L1-L30)
- [lib/aiComment.js:1-30](file://lib/aiComment.js#L1-L30)
- [netlify/functions/comment.js:1-20](file://netlify/functions/comment.js#L1-L20)

**章节来源**
- [app.js:1-100](file://app.js#L1-L100)
- [server.js:1-100](file://server.js#L1-L100)

## 核心组件

### 数据存储常量定义

系统使用统一的存储常量来管理不同类型的数据：

```mermaid
classDiagram
class STORAGE {
+string RECORDS : "myscore_v51_records"
+string CUSTOM : "myscore_v51_custom"
+string AUTH : "myscore_auth"
+string USER_MODE : "myscore_user_mode"
+string LOCAL_AI_USAGE : "myscore_local_ai_usage"
}
class AI_STYLES {
+string storm : "风暴"
+string sun : "暖阳"
+string cold : "冷锋"
+string rain : "阵雨"
}
STORAGE --> AI_STYLES : "使用"
```

**图表来源**
- [app.js:2-8](file://app.js#L2-L8)
- [app.js:751-757](file://app.js#L751-L757)

系统支持四种用户模式：
- **local模式**：本地用户，数据仅存储在浏览器本地
- **loggedin模式**：已登录用户，支持云端同步

**章节来源**
- [app.js:2-8](file://app.js#L2-L8)
- [app.js:38-69](file://app.js#L38-L69)

### 同步定时器管理

系统使用定时器机制实现智能同步：

```mermaid
sequenceDiagram
participant Client as 客户端
participant Timer as 同步定时器
participant Server as 服务器
Client->>Client : 数据变更
Client->>Timer : scheduleCloudSync()
Timer->>Timer : setTimeout(pushToCloud, 500ms)
Note over Timer : 500ms延迟避免频繁同步
Timer->>Server : pushToCloud()
Server-->>Timer : 同步成功
Timer->>Timer : 清除定时器
```

**图表来源**
- [app.js:666-670](file://app.js#L666-L670)
- [app.js:672-687](file://app.js#L672-L687)

**章节来源**
- [app.js:666-687](file://app.js#L666-L687)

## 架构概览

MyScore 的数据同步架构采用客户端-服务器双层设计：

```mermaid
graph TB
subgraph "客户端应用"
A[用户界面]
B[数据存储层]
C[同步引擎]
D[错误处理]
end
subgraph "云端服务"
E[认证服务]
F[数据存储]
G[AI评论服务]
H[静态资源]
end
subgraph "外部服务"
I[Cloudflare Turnstile]
J[Resend 邮件服务]
K[第三方AI服务]
end
A --> B
B --> C
C --> E
C --> F
C --> G
E --> I
E --> J
G --> K
D -.-> C
D -.-> E
D -.-> F
```

**图表来源**
- [app.js:1042-1068](file://app.js#L1042-L1068)
- [server.js:275-502](file://server.js#L275-L502)

## 详细组件分析

### gatherAllLocalStorage 函数工作原理

gatherAllLocalStorage 函数负责收集客户端所有需要同步的数据：

```mermaid
flowchart TD
Start([函数调用]) --> CheckAuth{检查用户认证}
CheckAuth --> |未认证| ReturnEmpty[返回空对象]
CheckAuth --> |已认证| CollectData[收集本地数据]
CollectData --> Records[读取成绩记录<br/>localStorage.RECORDS]
CollectData --> Custom[读取自定义考试<br/>localStorage.CUSTOM]
CollectData --> Goals[读取目标设置<br/>myscore_goals]
CollectData --> AiStyle[读取AI风格<br/>myscore_ai_style]
CollectData --> TutHistory[读取导师历史<br/>myscore_tutuer_history]
Records --> BuildObj[构建数据对象]
Custom --> BuildObj
Goals --> BuildObj
AiStyle --> BuildObj
TutHistory --> BuildObj
BuildObj --> ReturnData[返回完整数据对象]
ReturnEmpty --> End([函数结束])
ReturnData --> End
```

**图表来源**
- [app.js:705-713](file://app.js#L705-L713)

该函数的实现特点：
- **数据完整性**：收集所有可同步的数据类型
- **错误处理**：使用 readStorageJson 函数进行安全的数据读取
- **默认值**：为每个数据类型提供合理的默认值

**章节来源**
- [app.js:705-713](file://app.js#L705-L713)
- [app.js:926-934](file://app.js#L926-L934)

### mergeCloudData 算法实现

mergeCloudData 函数实现了复杂的双向数据合并策略：

```mermaid
flowchart TD
Start([接收云端数据]) --> CheckRecords{检查成绩记录}
CheckRecords --> |存在且有数据| MergeRecords[合并成绩记录]
CheckRecords --> |不存在或为空| CheckCustom{检查自定义数据}
MergeRecords --> ExtractCloudIds[提取云端记录ID]
ExtractCloudIds --> CompareLocal[比较本地记录]
CompareLocal --> AddMissing[添加本地缺失记录]
AddMissing --> SortRecords[按ID降序排序]
SortRecords --> SaveRecords[保存合并结果]
CheckCustom --> |存在| MergeCustom[合并自定义数据]
CheckCustom --> |不存在| CheckGoals{检查目标数据}
MergeCustom --> SaveCustom[保存合并结果]
CheckGoals --> |存在| MergeGoals[合并目标数据]
CheckGoals --> |不存在| CheckAiStyle{检查AI风格}
MergeGoals --> SaveGoals[保存合并结果]
CheckAiStyle --> |存在| UpdateAiStyle[更新AI风格]
CheckAiStyle --> |不存在| RenderDashboard[渲染仪表盘]
UpdateAiStyle --> RenderDashboard
SaveRecords --> RenderDashboard
SaveCustom --> RenderDashboard
SaveGoals --> RenderDashboard
RenderDashboard --> End([合并完成])
```

**图表来源**
- [app.js:715-743](file://app.js#L715-L743)

数据合并的核心策略：

1. **成绩记录合并**：
   - 云端优先：保留云端存在的记录
   - 本地补充：添加云端不存在的本地记录
   - 去重处理：使用ID集合避免重复
   - 排序规则：按ID降序排列

2. **自定义数据合并**：
   - 使用 Object.assign 进行浅拷贝合并
   - 云端数据优先于本地数据

3. **目标数据合并**：
   - 解析本地JSON数据
   - 云端优先合并策略

4. **AI风格同步**：
   - 直接替换本地设置
   - 更新当前AI风格状态

**章节来源**
- [app.js:715-743](file://app.js#L715-L743)

### pushToCloud 异步处理流程

pushToCloud 函数负责将本地数据推送到云端：

```mermaid
sequenceDiagram
participant Client as 客户端
participant Timer as 同步定时器
participant Server as 服务器
participant DB as 数据库
Client->>Timer : scheduleCloudSync()
Timer->>Timer : setTimeout(500ms)
Timer->>Client : pushToCloud()
Client->>Client : gatherAllLocalStorage()
Client->>Server : PUT /api/sync (含JWT令牌)
Server->>Server : 验证JWT令牌
Server->>DB : saveUserData(userId, data)
DB-->>Server : 存储成功
Server-->>Client : {ok : true}
Note over Client,Server : 成功路径
Client->>Client : scheduleCloudSync()
Timer->>Timer : setTimeout(500ms)
Note over Client : 错误处理
Client->>Client : 捕获异常
Client->>Client : showAiToast("云同步失败")
```

**图表来源**
- [app.js:672-687](file://app.js#L672-L687)
- [server.js:469-502](file://server.js#L469-L502)
- [lib/db.js:192-196](file://lib/db.js#L192-L196)

**章节来源**
- [app.js:672-687](file://app.js#L672-L687)
- [server.js:469-502](file://server.js#L469-L502)

### pullFromCloud 异步处理流程

pullFromCloud 函数负责从云端拉取数据：

```mermaid
sequenceDiagram
participant Client as 客户端
participant Server as 服务器
participant DB as 数据库
Client->>Server : GET /api/sync (含JWT令牌)
Server->>Server : 验证JWT令牌
Server->>DB : getUserData(userId)
DB-->>Server : 返回用户数据
Server-->>Client : {ok : true, data : userData}
Client->>Client : mergeCloudData(data)
Client->>Client : renderDashboard()
Note over Client : 错误处理
Client->>Client : 捕获401状态
Client->>Client : forceLogout()
Client->>Client : showAiToast("云端数据拉取失败")
```

**图表来源**
- [app.js:689-703](file://app.js#L689-L703)
- [server.js:484-487](file://server.js#L484-L487)
- [lib/db.js:198-206](file://lib/db.js#L198-L206)

**章节来源**
- [app.js:689-703](file://app.js#L689-L703)
- [server.js:484-487](file://server.js#L484-L487)

### 本地存储策略

系统采用分层存储策略：

```mermaid
graph TB
subgraph "本地存储层"
A[localStorage - 主要数据存储]
B[IndexedDB - 大容量数据]
C[SessionStorage - 临时数据]
end
subgraph "数据分类"
D[成绩记录 - RECORDS]
E[自定义考试 - CUSTOM]
F[用户偏好 - USER_MODE]
G[AI使用统计 - LOCAL_AI_USAGE]
H[认证信息 - AUTH]
end
A --> D
A --> E
A --> F
A --> G
A --> H
subgraph "存储优化"
I[JSON序列化]
J[错误处理]
K[默认值提供]
L[类型验证]
end
D --> I
E --> I
F --> I
G --> I
H --> I
I --> J
J --> K
K --> L
```

**图表来源**
- [app.js:2-8](file://app.js#L2-L8)
- [app.js:926-934](file://app.js#L926-L934)

**章节来源**
- [app.js:2-8](file://app.js#L2-L8)
- [app.js:926-934](file://app.js#L926-L934)

### 本地 AI 使用次数限制

系统实现了严格的本地AI使用限制机制：

```mermaid
flowchart TD
Start([AI请求]) --> CheckMode{检查用户模式}
CheckMode --> |local模式| CheckLimit[检查本地AI限制]
CheckMode --> |loggedin模式| CheckRemote[检查远程限制]
CheckLimit --> GetUsage[获取当日使用统计]
GetUsage --> CompareLimit{是否达到5次限制}
CompareLimit --> |是| ShowError[显示错误提示]
CompareLimit --> |否| IncrementUsage[增加使用计数]
IncrementUsage --> SaveUsage[保存使用统计]
SaveUsage --> Proceed[继续AI请求]
CheckRemote --> RemoteCheck[检查云端限制]
RemoteCheck --> RemoteProceed[继续AI请求]
ShowError --> End([请求失败])
Proceed --> End
RemoteProceed --> End
```

**图表来源**
- [app.js:40-69](file://app.js#L40-L69)
- [server.js:118-133](file://server.js#L118-L133)

**章节来源**
- [app.js:40-69](file://app.js#L40-L69)
- [server.js:118-133](file://server.js#L118-L133)

### 用户模式切换机制

系统支持动态的用户模式切换：

```mermaid
stateDiagram-v2
[*] --> LocalMode : 未登录状态
[*] --> LoggedInMode : 已登录状态
LocalMode --> LoggedInMode : 用户登录
LoggedInMode --> LocalMode : 用户登出
LocalMode --> LocalMode : 继续本地模式
LoggedInMode --> LoggedInMode : 继续登录模式
state LoggedInMode {
[*] --> PullData : 拉取云端数据
PullData --> MergeData : 合并数据
MergeData --> PushData : 推送合并结果
PushData --> [*] : 同步完成
}
state LocalMode {
[*] --> LocalOnly : 仅本地数据
LocalOnly --> [*] : 正常使用
}
```

**图表来源**
- [app.js:42-49](file://app.js#L42-L49)
- [app.js:379-388](file://app.js#L379-L388)

**章节来源**
- [app.js:42-49](file://app.js#L42-L49)
- [app.js:379-388](file://app.js#L379-L388)

## 依赖关系分析

### 核心依赖图

```mermaid
graph TB
subgraph "客户端依赖"
A[app.js]
B[auth.js]
C[db.js]
D[aiComment.js]
end
subgraph "服务器端依赖"
E[server.js]
F[lib/auth.js]
G[lib/db.js]
H[lib/aiComment.js]
end
subgraph "外部依赖"
I[Cloudflare Turnstile]
J[Resend API]
K[第三方AI服务]
end
A --> B
A --> C
A --> D
E --> F
E --> G
E --> H
B --> I
B --> J
H --> K
F --> G
H --> E
```

**图表来源**
- [app.js:1-50](file://app.js#L1-L50)
- [server.js:1-15](file://server.js#L1-L15)
- [lib/auth.js:1-15](file://lib/auth.js#L1-L15)
- [lib/aiComment.js:1-6](file://lib/aiComment.js#L1-L6)

### 数据流依赖

```mermaid
sequenceDiagram
participant UI as 用户界面
participant Sync as 同步引擎
participant Auth as 认证服务
participant Data as 数据存储
participant Cloud as 云端服务
UI->>Sync : 数据变更事件
Sync->>Sync : scheduleCloudSync()
Sync->>Data : gatherAllLocalStorage()
Sync->>Auth : 验证JWT令牌
Sync->>Cloud : pushToCloud()
Cloud->>Data : saveUserData()
Cloud->>UI : pullFromCloud()
UI->>Data : mergeCloudData()
Data->>UI : renderDashboard()
```

**图表来源**
- [app.js:666-743](file://app.js#L666-L743)
- [server.js:469-502](file://server.js#L469-L502)

**章节来源**
- [app.js:666-743](file://app.js#L666-L743)
- [server.js:469-502](file://server.js#L469-L502)

## 性能考虑

### 同步性能优化

系统采用了多项性能优化策略：

1. **延迟同步**：500ms延迟避免频繁同步
2. **增量更新**：仅在数据变更时触发同步
3. **批量处理**：一次性推送所有本地数据
4. **缓存策略**：本地数据缓存减少重复读取

### 存储性能优化

```mermaid
flowchart LR
subgraph "存储优化策略"
A[JSON序列化]
B[错误处理]
C[默认值提供]
D[类型验证]
end
subgraph "性能指标"
E[读取速度]
F[写入速度]
G[内存使用]
H[存储空间]
end
A --> E
B --> F
C --> G
D --> H
E --> I[优化效果]
F --> I
G --> I
H --> I
```

**图表来源**
- [app.js:926-934](file://app.js#L926-L934)

### 网络性能优化

- **超时控制**：AI请求30秒超时限制
- **重试机制**：网络异常时的自动重试
- **错误隔离**：单个请求失败不影响整体系统

## 故障排除指南

### 常见同步问题

| 问题类型 | 症状 | 可能原因 | 解决方案 |
|---------|------|----------|----------|
| 同步失败 | 云同步失败提示 | 网络连接问题 | 检查网络连接，重试同步 |
| 数据不一致 | 本地和云端数据不同步 | JWT令牌过期 | 重新登录获取新令牌 |
| 同步延迟 | 数据更新后未立即同步 | 定时器延迟 | 等待500ms或手动触发同步 |
| 存储不足 | 本地存储空间不足 | localStorage已满 | 导出数据清理空间 |

### 错误处理机制

系统实现了多层次的错误处理：

```mermaid
flowchart TD
Start([请求发起]) --> TryRequest[尝试执行请求]
TryRequest --> CheckResponse{检查响应状态}
CheckResponse --> |成功| Success[处理成功响应]
CheckResponse --> |401未授权| Handle401[处理未授权错误]
CheckResponse --> |网络错误| HandleNetwork[处理网络错误]
CheckResponse --> |其他错误| HandleOther[处理其他错误]
Handle401 --> ForceLogout[强制登出]
HandleNetwork --> ShowToast[显示错误提示]
HandleOther --> ShowToast
Success --> End([请求完成])
ForceLogout --> End
ShowToast --> End
```

**图表来源**
- [app.js:689-703](file://app.js#L689-L703)
- [app.js:672-687](file://app.js#L672-L687)

**章节来源**
- [app.js:689-703](file://app.js#L689-L703)
- [app.js:672-687](file://app.js#L672-L687)

### 调试技巧

1. **开发者工具**：使用浏览器开发者工具监控网络请求
2. **日志输出**：检查控制台中的错误信息
3. **状态检查**：验证localStorage中的数据完整性
4. **令牌验证**：确认JWT令牌的有效性

## 结论

MyScore 的数据同步系统设计精良，实现了以下关键特性：

1. **可靠的数据一致性**：通过双向合并算法确保本地和云端数据的一致性
2. **智能的同步策略**：采用延迟同步和增量更新优化性能
3. **完善的错误处理**：多层次的错误处理和恢复机制
4. **灵活的用户模式**：支持本地和云端两种使用模式
5. **严格的安全控制**：JWT认证和数据加密保护用户隐私

系统在保证用户体验的同时，确保了数据的安全性和完整性。通过合理的架构设计和实现细节，MyScore 为用户提供了稳定可靠的数据同步服务。

## 附录

### 配置参数说明

| 参数名称 | 类型 | 默认值 | 描述 |
|---------|------|--------|------|
| LOCAL_AI_DAILY_LIMIT | number | 5 | 本地AI使用每日限制 |
| syncTimer | timeout | null | 同步定时器实例 |
| TURNSTILE_SITE_KEY | string | "" | Cloudflare验证密钥 |
| COMMENT_API_ENDPOINT | string | "/api/comment" | AI评论API端点 |

### API端点说明

| 端点 | 方法 | 功能 | 认证要求 |
|------|------|------|----------|
| /api/sync | GET | 拉取用户数据 | JWT |
| /api/sync | PUT | 推送用户数据 | JWT |
| /api/auth/send-code | POST | 发送验证码 | 无需 |
| /api/auth/login-code | POST | 邮箱登录 | 无需 |
| /api/auth/register | POST | 用户注册 | 无需 |
| /api/auth/login-password | POST | 密码登录 | 无需 |
| /api/auth/profile | GET | 获取用户资料 | JWT |
| /api/auth/profile | PUT | 更新用户资料 | JWT |

### 数据模型

```mermaid
erDiagram
USER {
number id PK
number uid UK
string email UK
string nickname
string avatar_seed
string bio
boolean is_admin
boolean is_beta
datetime created_at
}
USER_DATA {
number user_id PK
json data
datetime updated_at
}
VERIFICATION_CODE {
string email PK
string code
datetime expires_at
number attempts
}
USER ||--|| USER_DATA : "拥有"
USER ||--o{ VERIFICATION_CODE : "注册"
```

**图表来源**
- [lib/db.js:50-108](file://lib/db.js#L50-L108)
- [lib/db.js:129-188](file://lib/db.js#L129-L188)